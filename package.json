{
  "name": "postcss-color-transform",
  "version": "0.0.0",
  "description": "PostCSS plugin to perform arbitrary transformations to colors.",
  "keywords": [
    "postcss",
    "css",
    "postcss-plugin",
    "color"
  ],
  "author": "Andrew Stiles <andrewbstiles@gmail.com>",
  "license": "MIT",
  "repository": "abstiles/postcss-color-transform",
  "bugs": {
    "url": "https://github.com/abstiles/postcss-color-transform/issues"
  },
  "homepage": "https://github.com/abstiles/postcss-color-transform",
  "dependencies": {
    "color": "^3.0.0",
    "postcss": "^6.0.16",
    "postcss-message-helpers": "^2.0.0",
    "reduce-function-call": "^1.0.2"
  },
  "devDependencies": {
    "eslint": "^4.18.2",
    "eslint-config-postcss": "^2.0.2",
    "jest": "^22.4.2"
  },
  "scripts": {
    "test": "jest && eslint *.js"
  },
  "eslintConfig": {
    "extends": "eslint-config-postcss/es5",
    "env": {
      "jest": true
    },
    "rules": {
      "indent": ["error", 2]
    }
  },
  "jest": {
    "testEnvironment": "node"
  }
}
